{
  "name": "Chrome KasmVNC",
  "build": {
    "dockerfile": "Dockerfile",
    "context": "."
  },
  "forwardPorts": [6901],
  "portsAttributes": {
    "6901": {
      "label": "Chrome Desktop",
      "onAutoForward": "openBrowser"
    }
  },
  "postAttachCommand": "if ! ss -tlnp | grep -q 6901; then bash /opt/start-vnc.sh > /tmp/vnc-startup.log 2>&1 & fi",
  "remoteUser": "vscode",
  "hostRequirements": {
    "cpus": 4,
    "memory": "8gb"
  }
}